# .github/workflows/ci.yml
name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout Code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Set up Node & Build React app
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Build React app
        run: npm run build

      # 3️⃣ Login to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 4️⃣ Build & Push Docker Image
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/react-cicd:latest

      # 5️⃣ Deploy to Kubernetes (CD)
      - name: Restart Kubernetes Deployment
        if: github.ref == 'refs/heads/main'
        run: |
          echo "🚀 Deployment step (will run on EKS later)"
          # Example for EKS (we’ll enable this once AWS cluster is ready)
          # aws eks update-kubeconfig --name react-cicd-cluster --region us-east-1
          # kubectl rollout restart deployment react-cicd-deployment
