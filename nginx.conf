server {
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    # This line tells nginx to serve index.html for all routes
    try_files $uri /index.html;
  }

  # Optional: reduce log noise for missing favicon etc.
  error_page 404 /index.html;
}
